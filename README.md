# SSN-search

User enters SSN record using three lines (name, address, and SSN).
User can keep entering entries until they enter "done" on a new line.

User can then search for entered entries by typing the name or SSN, until they type "done".

This program uses maps and vectors to store and retrieve the data quickly.
